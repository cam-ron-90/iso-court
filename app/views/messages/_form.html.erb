<%= simple_form_for [@chatroom.group, @chatroom, @message ], remote: true do |f| %>
  <%= f.input :content, label: false, placeholder: "Message ##{@chatroom.name}" %>
  <input name="authenticity_token" value="<%=form_authenticity_token%>", type="hidden">
<% end %>
